// We can insert, or interpolate, variables into strings using template literals.

const myPet = "armadilo";
console.log(`I own a pet ${myPet}.`);

const myName = "Hacker";
let myCity = "New York";
console.log(`My name is ${myName}. My favorite city is ${myCity}.`);
