// In programming we can perform a task based on a condition using an if statement.

if (true) {
  console.log(`This message will print!`);
}
/**
 * The if statement is composed of:
 * The if keyword followed by a set of parentheses () which is followed by a code block, or block statement, indicated by a set of curly braces{}.
 * Inside the parentheses (), a condition is provided that evaluates to true or false.
 * If the condition evaluates to true, the code inside the curly braces {} runsm or executes.
 * In the condition evaluates to false, the block won't execute
 */

let sale = true;
sale = false;
if (sale) {
  console.log(`Time to buy!`);
}
