/**
 * A loop is a programming tool that repeats a set of instruction until a specific condition, called stopping condition is reached.
 * Iterate is a generic term that referring to loops. it's simply means 'to repeat'
 * Loops allow us to create efficient code that automates processes to make scalable, manageable programs.
 */
