// With simple modifications to the expressions, we can make our loop run backward!
/**
 * To run backwad for loop, we must:
 * Set the iterator variable to the highest disered value in the initialization expression.
 * Set the stopping condition for when the iterator variable is less than the desired amount.
 * The itarator should decrease in inervals after each interation.
 */

// Exercise:
for (let counter = 3; counter >= 0; counter--) {
  console.log(counter);
}
